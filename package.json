{
  "name": "portofolio-fn",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/local-auth-server.mjs --host 0.0.0.0 --port 4321",
    "dev:vite": "vite --host 127.0.0.1 --port 4321",
    "assets:manifest": "node scripts/generate-portfolio-manifest.mjs",
    "prebuild": "npm run assets:manifest",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 4321",
    "test": "node --test"
  },
  "dependencies": {
    "google-auth-library": "^10.3.0"
  },
  "devDependencies": {
    "vite": "^6.4.2"
  }
}
